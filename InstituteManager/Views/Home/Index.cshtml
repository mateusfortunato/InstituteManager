@page
@model IEnumerable<InstituteManager.Models.Institute>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title> Index </title>
</head>
<body>
<p>
    <a asp-action="Create"> Create new </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InstituteID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adress)
            </th>
        <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayNameFor(modelItem => item.InstituteID)
                </td>
                <td>
                    @Html.DisplayNameFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayNameFor(modelItem => item.Adress)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.InstituteID}) |
                    @Html.ActionLink("Details", "Details", new { id=item.InstituteID}) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.InstituteID})
                </td>
            </tr>
        }
    </tbody>
</body>
</html>